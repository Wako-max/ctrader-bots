# Tests

`test_market_state_expansion_hysteresis.py` exercises the deterministic state-transition contract for contraction/neutral/expansion. The cTrader source itself is compiled separately by the repository workflow using the pinned `cTrader.Automate` package.
