# Safety

This indicator uses `AccessRights.None` and contains no broker order/position action path. The contraction/expansion feature only changes market-state observation and confidence evidence. The V1 execution intent mapping is preserved verbatim in behavior: dynamics do not create an order action, side, style, or permission.
