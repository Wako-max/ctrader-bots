# Contract compatibility

The contract family remains `tos.market-state.v1`. Version 1.2.0 adds result fields and one numeric indicator output without renaming or removing existing fields/outputs. The execution-intent contract remains `tos.market-state.execution-intent.v1` and its mapping is unchanged.
