# Notes

The `ExpansionState` name follows the architectural decision already recorded for TOS: contraction/neutral/expansion is a market-dynamics dimension. The implementation keeps the existing `tos.market-state.v1` contract family and increments the agent implementation version to `1.2.0` because the new fields and output are additive.
